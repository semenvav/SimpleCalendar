import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true,
    proxy: {
      // In development the Kotlin server runs separately; proxying keeps the frontend
      // on a single origin so nothing needs CORS in production.
      '/api': {
        target: process.env.SC_BACKEND ?? 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
  },
})
